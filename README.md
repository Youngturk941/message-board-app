# Message Board

A minimal React message board UI configured with RainbowKit and wagmi for Monad testnet.

Set `VITE_WALLETCONNECT_PROJECT_ID` in `.env` for WalletConnect support.

## Run locally

```bash
npm install
npm run dev
```
